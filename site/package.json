{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy:cf": "astro build && wrangler deploy",
    "deploy:vercel": "astro build && vercel deploy --prod --yes",
    "deploy": "bun run deploy:cf && bun run deploy:vercel"
  },
  "dependencies": {
    "astro": "^7.2.4"
  }
}